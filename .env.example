# IMDb data via OMDb (free key: https://www.omdbapi.com/apikey.aspx)
OMDB_API_KEY=

# Cloud accounts + library sync (same Supabase project as Hermes/Sillage is fine)
# Create a private Storage bucket named "sequel" (API auto-creates if allowed)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: dedicated HMAC secret for session tokens (defaults to service role key)
# SEQUEL_SESSION_SECRET=

# Optional later: AI-powered recommendations via xAI
# XAI_API_KEY=
