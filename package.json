{
  "name": "sequel",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public/assets && cp index.html styles.css app.js data.js public/ && cp -R assets/. public/assets/ && echo 'built public/'"
  },
  "engines": {
    "node": ">=18"
  }
}
